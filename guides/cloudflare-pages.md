# Cloudflare Pages: TFWS Adoption Guide

This guide explains how to publish TFWS v2 trust signals using **Cloudflare Pages**.

No server configuration is required.

---

## 1) Project structure

Your Pages project should contain a `.well-known` directory at the root:

