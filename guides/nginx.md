# nginx: TFWS Adoption Guide

This guide explains how to publish TFWS v2 trust signals using a standard **nginx** web server.

This assumes you already have a working HTTPS-enabled nginx setup.

---

## 1) Directory structure

Your website root must contain a `.well-known` directory:

